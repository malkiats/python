1. uv python
- If Python is already installed on your system, uv will detect and use it without configuration. 
- However, uv can also install and manage Python versions.
- uv automatically installs missing Python versions as needed — you don't need to install Python to get started.